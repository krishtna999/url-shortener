<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- PAGE settings -->
    <title>Url Shortener</title>
    <!-- CSS dependencies -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" type="text/css">
    <link rel="stylesheet" href="/css/colorful.css">
    <!-- Script: Navbar on-top -->
    <script src="/js/navbar-ontop.js"></script>
</head>

<body>
  <%- include header.ejs %>
    <div class="py-5 text-center cover d-flex flex-column bg-dark">
        <div class="container my-auto">
                    <% if(urlPath.length!=0){%>
                        <div class="row">
                            <div class="col-md-12">
                        <h1 class="display-1 mb-0 mt-5 big-title">Oops !</h1>
                </div>
            </div>
            <div class="row">
                <div class="mx-auto col-lg-8 col-md-8">
                    <h3 class="mb-4">The requested URL was not found/already exists !</h3>
                    <p>But, we have preloaded the URL in the form incase you wanted to create it<br></p>
                </div>
            </div>
            <%}

        else{%>
            <%- include titleBanner.ejs %>    
    <p>with Multiple URL support and Hassle Free Direct Adding !<br></p>
    <%}%>
        <div class="row">
            <div class="col-md-12">
                <form class="" action="/newUrl" method="POST">
                    <div class="row">
                        <div class="col-md-12"></div>
                    </div>
                    <div class="field_wrapper">
                        <div class="form-group"><input name="longUrl[]" type="url" class="form-control" placeholder="Url to be shortened" required></div>
                    </div>
                    <a href="javascript:void(0);" class="btn navbar-btn btn-sm add_button" title="Add field">Add Another
          URL</a>
                    <!-- <br> -->
                    <!-- <label>Customized Shortened URL</label> -->
                    <div class="form-group"> <input type="text" name="custom" class="form-control" placeholder="Custom Alias for shortening" value="<%=urlPath%>" pattern="[A-Za-z0-9]*" </div><small class="form-text text-muted">Custom URLs are optional. If not given, we'll generate one for you
                    </small></div>      
                        <button type="submit" class="btn btn-primary">Shorten !</button>
                </form>
                </div>
            </div>
            <%- include why.ejs %>
            <%- include footer.ejs %>
</body>

</html>